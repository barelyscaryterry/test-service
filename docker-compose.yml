services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: test-service-dynamodb-local
    ports:
      - "8000:8000"
    command: ["-jar", "DynamoDBLocal.jar", "-inMemory", "-sharedDb"]
